export const BUTTON_TYPES = ["button", "submit", "reset"] as const;

export const BUTTON_VARIANT_TYPES = [
  "primary",
  "secondary",
  "outline",
  "plain",
  "danger",
  "link",
] as const;

export const POSITION_TYPES = ["left", "center", "right"];

export const DROPDOWN_VARIANT_TYPES = ["outline", "plain", "primary"] as const;
