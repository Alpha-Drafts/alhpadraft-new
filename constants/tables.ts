export const TABLE_ACTION_TYPES = [
  "default",
  "outline",
  "danger",
  "success",
  "warning",
  "plain",
] as const;

export const DEFAULT_SORT_OPTIONS = ["date", "name"];

export const PROJECT_SORT_OPTIONS = ["createdAt", "updatedAt", "dueDate"];

export const PROJECT_ORDER_OPTIONS = ["desc", "asc"];

export const ITEMS_PER_PAGE = 20;
